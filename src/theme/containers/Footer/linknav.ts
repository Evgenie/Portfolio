import { TCSSObjProps } from "../../../definitions";

export const blocks: TCSSObjProps = {
   core: {
      marginLeft: '2rem',
   },
   mobile: {},
   laptop: {},
   desktop: {},
};

export default blocks;